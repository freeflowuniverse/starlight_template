---
import { CsvChartDemo } from './CsvChartDemo';
import * as fs from 'node:fs';
import * as path from 'node:path';

const csvPath = path.join(process.cwd(), 'src', 'content', 'test.csv');
const csvContent = fs.readFileSync(csvPath, 'utf-8');
---

<CsvChartDemo client:load csvContent={csvContent} />
